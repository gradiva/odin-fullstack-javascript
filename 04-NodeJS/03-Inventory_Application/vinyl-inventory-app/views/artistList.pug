extends layout

block content
  h1= title
  
  ul
    each artist in artistList
      li 
        a(href=artist.url) #{artist.name} 
        |  (#{artist.lifespan})

    else
      li There are no artists.